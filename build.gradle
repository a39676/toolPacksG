apply plugin: 'java-library'
apply plugin: 'java'

version = '0.0.3.2'
// JDK version source compatibility
compileJava {
	sourceCompatibility = '1.8'
	targetCompatibility = '1.8'
}

[compileJava,compileTestJava,javadoc]*.options*.encoding = 'UTF-8'

//create a single Jar with all dependencies
task fatJar(type: Jar) {
	manifest {
        attributes 'Implementation-Title': 'toolPacksG',
			'Implementation-Version': version
    }
    
    //baseName = project.name + '-all'
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}

repositories {
    flatDir {
        dirs 'src/libs/'
    }
    jcenter()
}

dependencies {
    // This dependency is exported to consumers, that is to say found on their compile classpath.
    api 'org.apache.commons:commons-math3:3.6.1'

    // This dependency is used internally, and not exposed to consumers on their own compile classpath.
    implementation 'com.google.guava:guava:21.0'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
    
    compile files('src/libs/commons-beanutils-1.9.2.jar')
    compile files('src/libs/commons-collections-3.2.2.jar')
    compile files('src/libs/commons-io-2.5.jar')
    compile files('src/libs/commons-lang-2.6.jar')
    compile files('src/libs/commons-logging-1.2.jar')
    compile files('src/libs/ezmorph-1.0.6.jar')
    compile files('src/libs/javax.mail-1.5.6.jar')
    compile files('src/libs/json-lib-2.4-jdk15.jar')
    compile files('src/libs/zt-zip-1.13.jar')
    compile files('src/libs/log4j-1.2.17.jar')
    compile files('src/libs/slf4j-api-1.7.21.jar')
    compile files('src/libs/slf4j-log4j12-1.7.21.jar')
<<<<<<< HEAD
    compile files('src/libs/activation-1.1.jar')
=======
    // https://mvnrepository.com/artifact/javax.activation/activation
	compile group: 'javax.activation', name: 'activation', version: '1.1.1'

>>>>>>> branch 'master' of https://github.com/a39676/toolPacksG.git
}